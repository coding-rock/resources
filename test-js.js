
// assign an value to array index that is greater than the array size
let sampleArray = [0,1,2];
sampleArray[5] = 5; // the console will add 2 empty slots into the array and then add 5 into the array. so the array size will be 6.
console.log(sampleArray);